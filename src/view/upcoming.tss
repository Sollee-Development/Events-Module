.event { repeat: data(getUpcomingEvents(3)); bind: iteration(); }
.event .description, .event h2.name, .event [data-user="all"] { display: none; }
#upcoming-events:data[getUpcomingEvents().item(0)=false] { content: "<p>There are no upcoming events currently.</p>"; format: html; }
